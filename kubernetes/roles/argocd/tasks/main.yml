- name: render argocd kustomization
  command: "{{ kustomize_cmd }} build {{ role_path }}/files"
  delegate_to: localhost
  register: manifest

- name: provision argocd
  shell: "echo {{ manifest.stdout | quote }} | {{ kubectl_cmd }} --kubeconfig {{ kubeconfig }} apply -f -"
